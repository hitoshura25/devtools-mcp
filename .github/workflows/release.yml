name: Release

on:
  # Mode 2: Publish to npm after CI completes successfully on main
  workflow_run:
    workflows: ["CI"]
    types: [completed]
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  release:
    runs-on: ubuntu-latest
    # Only run if CI workflow succeeded
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    # Enable OIDC for trusted publishing to npm
    permissions:
      id-token: write
      contents: write  # Needed for changesets to push git tags
    steps:
      - uses: actions/checkout@v4
        with:
          # Checkout the commit from the completed CI workflow
          ref: ${{ github.event.workflow_run.head_sha }}

      - uses: pnpm/action-setup@v4

      - name: Check package registry status
        id: check-packages
        run: |
          echo "🔍 Checking package status on npm registry..."
          echo ""

          # Get all workspace packages
          PACKAGES=$(pnpm list --recursive --depth -1 --json 2>/dev/null | jq -r '.[] | select(.private != true) | .name')

          if [ -z "$PACKAGES" ]; then
            echo "ℹ️  No public packages found"
            exit 0
          fi

          # Check each package on npm registry
          NEW_PACKAGES=""
          while IFS= read -r PKG; do
            if [ -n "$PKG" ]; then
              echo "Checking $PKG..."
              if npm view "$PKG" version &>/dev/null; then
                echo "  ✓ Exists on npm (will use OIDC if configured)"
              else
                echo "  ⚠️  NEW package (will use NPM_TOKEN for first publish)"
                NEW_PACKAGES="${NEW_PACKAGES}${PKG} "
              fi
            fi
          done <<< "$PACKAGES"

          if [ -n "$NEW_PACKAGES" ]; then
            echo ""
            echo "new_packages=${NEW_PACKAGES% }" >> $GITHUB_OUTPUT
            echo "has_new_packages=true" >> $GITHUB_OUTPUT
          else
            echo "has_new_packages=false" >> $GITHUB_OUTPUT
          fi

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          registry-url: 'https://registry.npmjs.org'

      - name: Configure npm authentication
        if: steps.check-packages.outputs.has_new_packages == 'true'
        run: |
          echo "🔐 Creating .npmrc with NPM_PUBLISH_TOKEN for new package(s)"
          echo "//registry.npmjs.org/:_authToken=${NPM_PUBLISH_TOKEN}" > $HOME/.npmrc
        env:
          NPM_PUBLISH_TOKEN: ${{ secrets.NPM_PUBLISH_TOKEN }}

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

      - name: Publish to npm
        id: publish
        uses: changesets/action@v1
        with:
          publish: pnpm release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Authentication handled by .npmrc (created above if package is new)
          # For existing packages, OIDC is used automatically

      - name: Notify about OIDC setup
        if: steps.check-packages.outputs.has_new_packages == 'true' && steps.publish.outputs.published == 'true'
        run: |
          echo ""
          echo "📦 New packages published! Configure OIDC trusted publishing:"
          echo ""

          # Read new packages into array safely
          IFS=' ' read -ra PACKAGES <<< "${{ steps.check-packages.outputs.new_packages }}"
          for PKG in "${PACKAGES[@]}"; do
            if [ -n "$PKG" ]; then
              echo "  $PKG"
              echo "  → https://www.npmjs.com/package/$PKG/settings"
            fi
          done

          echo ""
          echo "📖 See docs/NPM_TRUSTED_PUBLISHING_SETUP.md for instructions"
          echo ""
